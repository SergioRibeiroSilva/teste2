import { FundoPretoDirective } from './fundo-preto.directive';

describe('FundoPretoDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoPretoDirective();
    expect(directive).toBeTruthy();
  });
});
